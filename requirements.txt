# MPC Catalog Visualization Toolkit
# Install: pip install -r requirements.txt

# Database
psycopg2-binary>=2.9

# Data manipulation
pandas>=2.1
numpy>=1.26
pyarrow>=15.0

# MPC designation pack/unpack (github.com/rlseaman/MPC_designations)
# Pinned to the 1.0.1 commit (same Python source as the 1.0.0 prod
# install; upstream 1.1.0 tightens packed-provisional validation and
# needs its own tested upgrade). Bump the SHA deliberately.
mpc-designation @ git+https://github.com/rlseaman/MPC_designations.git@9196621803dce9d6bdc3e3b9d89db130281653ca#subdirectory=python

# Web application framework
dash>=2.14

# Production WSGI server. Used in deployment (--waitress flag in
# scripts/start-dashboard*.sh); dev runs without it on Flask's
# built-in server.
waitress>=3.0

# Interactive charting
plotly>=5.18

# Static image export from Plotly (PNG/SVG)
kaleido>=0.2

# Notebook server
jupyterlab>=4.0

# XML processing (ADES export). Pinned to 6.1+ because 6.1.0 disabled
# resolve_entities by default, mitigating CVE-2026-41066 (XXE) for any
# future code path that might parse untrusted XML.
lxml>=6.1.0

# Security floors for transitive dependencies flagged by pip-audit on
# 2026-09-09 (tornado via jupyterlab; urllib3 via requests; soupsieve
# via beautifulsoup4). None are on the dashboard's request path, but
# they are in the venv. Re-run `pip-audit` quarterly.
tornado>=6.5.8
urllib3>=2.7.0
soupsieve>=2.8.4
